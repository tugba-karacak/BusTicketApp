<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" asp-action="Index" asp-controller="Home">BusTicketApp</a>
        <ul class="navbar-nav ml-auto">

            @if (ViewBag.Control == true)
            {

                @if (ViewBag.IsAdmin == true)
                {
                    <li class="nav-item active">
                        <a class="nav-link" asp-action="Index" asp-controller="Admin"> Biletleri Görüntüle</a>
                    </li>
                }
                else if (ViewBag.IsMember == true)
                {
                    <li class="nav-item active">
                        <a class="nav-link text-right" asp-action="Index" asp-controller="Voyage">Sefer Ara</a>
                    </li>
                }

                <li class="nav-item active">
                    <a class="nav-link text-right" asp-action="Logout" asp-controller="Account">Çıkış Yap</a>

                </li>

            }
            else
            {
                <li class="nav-item active">
                    <a class="nav-link text-right" asp-action="Login" asp-controller="Account">Giriş Yap</a>

                </li>

            }



        </ul>

        </div>
    </nav>